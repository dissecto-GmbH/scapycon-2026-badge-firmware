# Copy to config/badge.conf and set a default display name (badge.conf is gitignored).
# In badge mode, open a USB serial terminal for the setup console (options 1–3):
# name, Wi-Fi SSID, and Wi-Fi key — all saved to the user partition.
name=Your Name
